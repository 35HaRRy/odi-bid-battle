{
  "name": "odi-bid-battle",
  "private": true,
  "packageManager": "pnpm@11.20.0",
  "type": "module",
  "engines": { "node": "24.x" },
  "workspaces": ["api", "web"],
  "scripts": {
    "dev": "docker compose up -d db && pnpm --parallel --filter odi-bid-battle-api --filter odi-bid-battle-web run dev",
    "typecheck": "pnpm --filter odi-bid-battle-api --filter odi-bid-battle-web run typecheck",
    "test": "pnpm --filter odi-bid-battle-api --filter odi-bid-battle-web run test",
    "db:up": "docker compose up -d db",
    "db:migrate": "docker compose exec -T db psql -U bidbattle -d bidbattle -v ON_ERROR_STOP=1 -f /docker-entrypoint-initdb.d/schema.sql"
  }
}
