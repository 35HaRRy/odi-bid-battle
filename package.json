{
  "name": "odi-bid-battle",
  "private": true,
  "type": "module",
  "workspaces": ["api", "web"],
  "scripts": {
    "typecheck": "npm run typecheck -w api && npm run typecheck -w web",
    "test": "npm run test -w api && npm run test -w web",
    "db:up": "docker compose up -d db",
    "db:migrate": "docker compose exec -T db psql -U bidbattle -d bidbattle -f /docker-entrypoint-initdb.d/schema.sql"
  }
}
